<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background shape with shadow -->
    <item android:top="2dp" android:right="2dp" android:bottom="4dp" android:left="4dp">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Background color -->
            <corners android:radius="8dp" /> <!-- Corner radius for a rounded appearance -->
        </shape>
    </item>

    <!-- Border (outline) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Border color -->
            <corners android:radius="8dp" /> <!-- Corner radius for a rounded appearance -->
            <stroke
                android:width="2dp"
            android:color="#CCCCCC" /> <!-- Border color -->
        </shape>
    </item>

    <!-- Actual content (ImageView) -->
    <item android:drawable="@drawable/default_profile_image" /> <!-- Your image resource -->
</layer-list>
